# UROP — Cardiomegaly classification from frontal chest X-rays
# Stage 2 (dataset acquisition & analysis) dependencies.
# Pinned loosely here; freeze exact versions with `pip freeze` before Stage 4.

# --- Stage 2: metadata analysis, integrity checks, splitting ---
numpy>=1.26
pandas>=2.2
pillow>=10.3
scikit-learn>=1.5          # StratifiedGroupKFold / GroupShuffleSplit
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66

# --- Torch: required by probe_vram.py; Stage 3+ training uses the same install ---
# INSTALLED 21/08/2026 on the local RTX 4060 (Ada, sm_89):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Verified working versions (see artifacts/stage2/reports/vram_probe.json):
torch==2.13.0             # +cu126
torchvision==0.28.0       # +cu126
# NOTE: torch 2.13 raises torch.AcceleratorError (not torch.cuda.OutOfMemoryError) on OOM.
# Code that catches OOM must match the message, not the exception class — see probe_vram._is_oom.

# --- Stage 3+ only (not yet installed) ---
# timm>=1.0                # EfficientNetV2 / MobileNetV3 weights
# optuna>=3.6
# xgboost>=2.1
# grad-cam>=1.5
